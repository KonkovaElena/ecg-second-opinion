services:
  ecg-second-opinion:
    build: .
    ports:
      - "3100:3100"
    environment:
      NODE_ENV: production
      PORT: 3100
    healthcheck:
      test: ["CMD", "wget", "-qO-", "http://localhost:3100/healthz"]
      interval: 30s
      timeout: 3s
      retries: 3
